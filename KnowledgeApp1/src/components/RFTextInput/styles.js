import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    rootInput: {
        borderColor: 'gray',
        borderWidth: 1,
        height: 40,
        padding: 10,
        marginBottom: 10,
        color: "white"
    },
    rootError: {
        color: 'red',
    },
});

export default styles;