import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mobilForm: {
        width: '100%',
        padding: 20
    },
    webForm: {

    },
    label: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#3c424c'
    }
});

export default styles;