import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    root: {
        borderColor: 'gray',
        borderWidth: 1,
        height: 40,
        padding: 10,
        marginBottom: 10,
        color: "white"
    }
});

export default styles;