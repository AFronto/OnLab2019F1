import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    errorTextStyle: {
        alignSelf: 'center',
        fontSize: 18,
        color: 'red',
        marginBottom: 5,
        fontFamily: "Segoe UI"
    },
    buttonText: {
        color: '#FFFFFF',
        fontFamily: "Segoe UI"
    }
});

export default styles;